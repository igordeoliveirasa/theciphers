language: cpp
compiler: clang
branches:
  only: master

before_install:
- autoconf --version
- autoreconf --install --force

notifications:
  recipients: igordeoliveirasa@gmail.com
  email:
    on_success: change
    on_failure: always
